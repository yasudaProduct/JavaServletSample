{
  "name": "java-servlet-sample-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers / Containers へデプロイするための入口 (Java 本体のビルドは Maven です)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260916.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.132.0"
  }
}
